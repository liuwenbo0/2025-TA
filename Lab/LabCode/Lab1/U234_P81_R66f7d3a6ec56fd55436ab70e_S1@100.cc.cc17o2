#include"SqList.h"
void insert(SqList& list, int num){
    int i,j,n;
    list.push_back(num);
    n=list.size();
    for(i=n-1;i>0;i--){
        if(list[i]<list[i-1]){
            j=list[i];
            list[i]=list[i-1];
            list[i-1]=j;
        }
        else  break;
    }
//在此处添加代码
}