#include"SqList.h"
using namespace std;  
void insert(SqList& list, int num){
    int n=list.size();
    list.push_back(num);
    int i;
    for(i=n-1;(i>=0)&&(list[i]>num);i--){
        list[i+1]=list[i];
    }
    list[i+1]=num;
}