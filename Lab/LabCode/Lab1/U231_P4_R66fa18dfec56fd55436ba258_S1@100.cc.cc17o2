#include "InsertionSort.h"
// 通过插入排序对int队列nums进行升序排序
// @param
// nums: 完整的待排序队列，最终排序的结果应存放在nums中
void InsertionSort::mysort(std::vector<int>& nums) {
    int i=0,j=0,q=0;
    int n=nums.size();
    for(i=0;i<n;i++){
        for(j=0;j<i;j++){
            if(nums[i]<nums[j]){
                int p=nums[i];
                for(q=i-1;q>=j;q--) nums[q+1]=nums[q];
                nums[j]=p;
            }
            else continue;
        }
    }
    // 请在这里完成你的代码
}