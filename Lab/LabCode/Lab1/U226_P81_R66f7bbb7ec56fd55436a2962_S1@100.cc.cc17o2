#include"SqList.h"
void insert(SqList& list, int num){
    int n=list.size();
    int m=0;
    for(int i=0;i<n;i++){
        if(num>=list[i]){
            m=i+1;
        }
         else{
            break;
         }
    }
     list.push_back(list[n-1]);
    for(int i=n-1;i>m;i--){
       list[i]=list[i-1];
    }
    list[m]=num;
    //在此处添加代码
}