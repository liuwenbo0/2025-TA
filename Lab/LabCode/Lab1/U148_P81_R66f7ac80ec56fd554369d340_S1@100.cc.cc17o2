#include"SqList.h"
void insert(SqList& list, int num) {
    int n = list.size();
    int i;
    for (i = 0; i < n; ++i) {
        if (list[i] > num) {
            break;
        }
    }
    list.insert(list.begin() + i, num);
}