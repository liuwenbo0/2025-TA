#include"SqList.h"
void insert(SqList& list, int num){
  

 
    // 找到插入位置
    auto it = list.begin();
    while (it != list.end() && *it < num) {
        ++it; // 向后移动迭代器
    }
    // 在找到的位置插入新元素
    list.insert(it, num);
}