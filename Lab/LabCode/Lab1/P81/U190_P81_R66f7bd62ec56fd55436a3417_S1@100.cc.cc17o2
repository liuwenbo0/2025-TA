#include"SqList.h"
void insert(SqList& list, int num) {
    list.push_back(0);
    for (int i = 0; i<int(list.size()); i++)
    {
        if (num <= list[i]||i==(int(list.size())-1))
        {
            for (int j = int(list.size() - 1); j > i; j--)
            {
                list[j] = list[j - 1];
            }
            list[i] = num;
            return;
        }
        else
        {
            continue;
        }
    }
}