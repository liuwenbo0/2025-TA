#include"SqList.h"
void insert(SqList& list, int num){
    //在此处添加代码
    list.push_back(0);
    for (int i = list.size() - 2; i >= 0; --i) {
        if(list[i] > num)list[i + 1] = list[i];
        else {
            list[i + 1] = num;
            break;
        }
    }
}