#include"SqList.h"
#include<iostream>
void insert(SqList& list, int num){
    int n=list.size();
    for(int i=n-1;i>=0;i--){
        if(num<=list[i]){
            list.resize(n+1);
            list[i+1]=list[i];
            list[i]=num;
        }
        if(num>list[n-1]){
            list.resize(n+1);
            list[n]=num;
        }
    }
}