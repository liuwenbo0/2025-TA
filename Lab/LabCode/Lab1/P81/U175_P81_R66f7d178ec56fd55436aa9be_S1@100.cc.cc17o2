#include"SqList.h"
void insert(SqList& list, int num){
   for(auto i=list.begin();i<=list.end();i++){
	if(*i>=num){
		list.insert(i,num);
		return;


	}
	else  if(i==list.end()){
        list.push_back(num);
	    return;
    }
}
}