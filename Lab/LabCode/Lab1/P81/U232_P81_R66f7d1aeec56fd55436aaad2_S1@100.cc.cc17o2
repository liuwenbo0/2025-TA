#include"SqList.h"

void insert(SqList& list, int num){
	int i;
	int n=list.size();
	n-=1;
	for(i=0;i<=n;i++){
		if(list[i]>=num){
			break;
		}
    }
	list.push_back(list[n]);
	for(int j=n;j>i;j--){
		list[j]=list[j-1];
	}
    list[i]=num;
			
		
	
}