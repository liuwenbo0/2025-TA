#include"SqList.h"
void insert(SqList& list, int num){
    list.push_back(num);
    int m=list.size()-1;
    int p;
    for(int i=0;i<m;i++){
        if(list[i]<num&&num<=list[i+1]){
            p=i+1;
            for(int j=m;j>i;j--){
                list[j]=list[j-1];
            }
            i=m+1;
        }
    }
    list[p]=num;
}