#include "InsertionSort.h"
// 通过插入排序对int队列nums进行升序排序
// @param
// nums: 完整的待排序队列，最终排序的结果应存放在nums中
void InsertionSort::mysort(std::vector<int>& nums) {
  int n = nums. size();
for (int a = 1; a< n; a++)
{int key = nums[a];
int b=a-1;
while (b>=0&&nums[b]>key)
    {nums[b+1]=nums[b];
    b=b-1;

    }nums[b+1]=key;
    
    }



    // 请在这里完成你的代码
}